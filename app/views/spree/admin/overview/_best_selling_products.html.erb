<div class='col-4'>
  <h5 class='bar-title'>
    <span><%= t('spree.simple_dash.best_selling_products') %></span>
  </h5>

  <div id='best_selling_products' style='width:80%; height:220px;'></div>

  <div id='pie-legend'>
    <% @best_selling_variants.each_with_index do |v,i| %>
      <div class='pie-element'>
        <i style='background-color:<%= @pie_colors[i] %>'>&nbsp;</i>
        <label><%= truncate v[0], length: 35 %></label>
        <span><%= number_with_delimiter v[1] %> <%= t('spree.simple_dash.units') %></span>
      </div>
    <% end %>
  </div>
</div>
